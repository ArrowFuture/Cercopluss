document.addEventListener('DOMContentLoaded', function () {
    // Inicialización de Bootstrap Carousel
    $('.carousel').carousel({
        interval: 3000 // Cambia de slide cada 3 segundos
    });
});
